# GSP Recruitment — Caddyfile context
# Served by the system Caddy instance (/etc/caddy/Caddyfile) at:
#
#   gsprecruitment.nl {
#       handle /api/* { reverse_proxy localhost:8000 }
#       handle /health { reverse_proxy localhost:8000 }
#       handle /openapi.json { reverse_proxy localhost:8000 }
#       handle {
#           root * /home/og/projects/gsp-recruitment/website
#           file_server
#           try_files {path} /index.html
#       }
#   }
